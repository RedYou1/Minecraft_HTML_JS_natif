class Cookable{
    /*
    store in cookable. normaly 10 each in CookTime give CookTo.
     */
    constructor(CookTo,CookTime) {
        this.CookTo = CookTo;
        this.CookTime = CookTime;
    }
}